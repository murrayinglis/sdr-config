#ifndef CLI_HPP
#define CLI_HPP

#include <string>

namespace cli
{
    // Declare the variables to hold option values
    //std::string option_find;
    extern std::string option_dump;
    extern std::string option_config;
    extern std::string option_test;    
}

#endif